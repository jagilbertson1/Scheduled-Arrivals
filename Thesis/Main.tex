\documentclass[12pt, twoside]{report}

% a4 paper (margins of 300mm don't make sense)
%\usepackage[a4paper, width = 150mm, top = 25mm, bottom = 25mm, bindingoffset = 6mm]{geometry}
\usepackage[a4paper, bindingoffset = 2mm]{geometry}

% set 1.5 line spacing
\usepackage[onehalfspacing]{setspace}

% create header and footer
\usepackage{fancyhdr}

% main page style
\pagestyle{fancy}
\fancyhead{}
\fancyhead[LE,RO]{Valuing Queues with a Dynamic Schedule}
\fancyhead[RE,LO]{\nouppercase{\leftmark}}
\fancyfoot{}
\fancyfoot[RE,LO]{\thepage}

% chapter page style
\fancypagestyle{chapter} {
	\fancyhead{}
	\renewcommand{\headrulewidth}{0pt}
	\fancyfoot{}
	\fancyfoot[RE,LO]{\thepage}
}

% redefine chapter command
\makeatletter
    \let\stdchapter\chapter
    \renewcommand*\chapter{%
    \@ifstar{\starchapter}{\@dblarg\nostarchapter}}
    \newcommand*\starchapter[1]{%
        \stdchapter*{#1}
        \thispagestyle{chapter}
        \markboth{\MakeUppercase{#1}}{}
    }
    \def\nostarchapter[#1]#2{%
        \stdchapter[{#1}]{#2}
        \thispagestyle{chapter}
    }
\makeatother

% get warning to change head height for header
\setlength{\headheight}{14.5pt} 

% limit table of contents to chapters and sections
\setcounter{tocdepth}{1}

% can use [nosep] or [noitemsep] in itemize and enumerate to reduce line spacing between items
\usepackage[shortlabels]{enumitem}

% add bibliography
\usepackage[backend = bibtex, style = authoryear, sorting = ynt, natbib]{biblatex}
\addbibresource{bibliography.bib}

% add figures
\usepackage{graphicx}
\graphicspath{ {Figures/} }

% format for algorithms
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}

% import packages
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{tabularx}
\usepackage{bbm}
\usepackage{multirow}

% define argmin and argmax
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}

\begin{document}

\input{Chapters/Title}

\input{Chapters/Declaration}

\input{Chapters/Abstract}

\tableofcontents

\input{Chapters/Introduction}

\input{Chapters/LiteratureReview}

\input{Chapters/StaticSchedule}

\input{Chapters/DynamicSchedule}

\input{Chapters/ValueDynamicSchedule}

\input{Chapters/SimulationStudies}

\input{Chapters/Conclusion}

\appendix

\input{Chapters/DynamicCostDerivation}

\printbibliography

\end{document}